# Plan discipline — moved to the harness

> **This file used to hold the standalone essay, and `maintainers/skills/plan-discipline/` held a
> copy-ready skill beside it. Both now live in one place**, so there is nothing left here to drift:
>
> **<https://github.com/tpierrain/use-case-driven-harness>** → `skills/plan-discipline/`
> (`SKILL.md` + `plan-discipline.md`, the full rationale).
>
> _(Moved 2026-08-20. On the maintainer's machines the skill is symlinked into `~/.claude/skills/` by
> that repo's `bootstrap.sh`, so it loads on demand in every project, including this one — which is
> what it never did while it sat here, under a path Claude Code does not scan.)_

## What stays in this repo

[`CONVENTIONS.md`](CONVENTIONS.md) §1, §2, §3, §3bis and §3ter remain **operative and repo-carried**:
they travel with any clone, and they say how the discipline applies *here* — the one door
([`plans/ACTIVE.md`](plans/ACTIVE.md)), the `## 📍 STATE` block every live plan opens with,
`maintainers/plans/**`, the ROADMAP as ordering authority, archiving on ship (§7). What left is only
the **project-agnostic** half, which had no reason to exist in two repos.

**Which one wins:** the harness, for the methodology itself. If a rule in `CONVENTIONS.md` and the
harness's `rules/plans.md` ever disagree about the *method*, change the harness first, then carry the
consequence here.
